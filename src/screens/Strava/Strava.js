import React, { useState } from "react";
import "./style.scss";

const Dashboard = () => {
  const foo = "bar";
  return (
    <div className="dashboard">
      <p>Dashboard</p>
    </div>
  );
};

export default Dashboard;
